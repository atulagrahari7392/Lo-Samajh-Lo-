{
  "name": "lo-samajh-lo",
  "version": "1.0.0",
  "private": true,
  "description": "Lo Samajh Lo - Modern Full-Stack LMS Platform & Admin System",
  "scripts": {
    "dev:server": "npm --prefix server run dev",
    "dev:client": "npm --prefix client run dev",
    "prisma:generate": "npm --prefix server run prisma:generate",
    "prisma:push": "npm --prefix server run prisma:push",
    "seed": "npm --prefix server run seed",
    "build:server": "npm --prefix server install && npm --prefix server run prisma:generate && npm --prefix server run prisma:push && npm --prefix server run build",
    "build:client": "npm --prefix client install && npm --prefix client run build",
    "build": "npm run build:server && npm run build:client && npm run seed",
    "build:all": "npm run build",
    "start": "npm --prefix server run start"
  },
  "keywords": [
    "lms",
    "education",
    "react",
    "typescript",
    "express",
    "prisma"
  ],
  "author": "Lo Samajh Lo",
  "license": "ISC"
}
